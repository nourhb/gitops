apiVersion: v1
kind: Service
metadata:
  name: {{ include "sanhome.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "sanhome.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: {{ include "sanhome.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - port: 80
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
