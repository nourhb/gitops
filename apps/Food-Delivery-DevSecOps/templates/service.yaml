apiVersion: v1
kind: Service
metadata:
  name: {{ include "Food-Delivery-DevSecOps.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "Food-Delivery-DevSecOps.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: {{ include "Food-Delivery-DevSecOps.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
